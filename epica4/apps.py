from django.apps import AppConfig

# Módulo epica4
class Epica4Config(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'epica4' #nombre del modulo
    verbose_name = "Gestión de Artículos" #nombre con el que será visible
